<section>
  <h1 class="sam-blog__page-title">{{ labels.not_found_title }}</h1>
  <p class="sam-blog__summary-excerpt">{{ labels.not_found_body }}</p>
  <p><a href="{{ site.url }}">{{ labels.all_articles }} <span aria-hidden="true">→</span></a></p>
</section>
